# Test Case ID: CN-N01
# Rule: container_security.container_network
# Test Type: negative
# Description: 使用桥接网络（安全）
# Expected Detection: false
# Expected Severity: N/A
# Code Type: safe

version: '3'
services:
  app:
    networks:
      - app_network  # 使用隔离的网络

networks:
  app_network:
    driver: bridge
